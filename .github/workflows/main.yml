name: Build APK

on:
  push:
    branches: [ fongmi ]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: build-apk-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: gradle

      - name: Install Python 3.10
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y python3.10 python3.10-dev python3.10-venv
          python3.10 --version

      - name: Ensure Android SDK 37
        run: |
          set -euo pipefail

          SDK_ROOT="${ANDROID_SDK_ROOT:-${ANDROID_HOME:-/usr/local/lib/android/sdk}}"
          SDKMANAGER="$SDK_ROOT/cmdline-tools/latest/bin/sdkmanager"

          if [ ! -x "$SDKMANAGER" ]; then
            SDKMANAGER="$(find "$SDK_ROOT/cmdline-tools" -type f -name sdkmanager 2>/dev/null | head -n 1 || true)"
          fi

          if [ -z "${SDKMANAGER:-}" ] || [ ! -x "$SDKMANAGER" ]; then
            echo "::error::sdkmanager not found under $SDK_ROOT"
            find "$SDK_ROOT" -maxdepth 4 -type f -name sdkmanager -print 2>/dev/null || true
            exit 1
          fi

          echo "Using sdkmanager: $SDKMANAGER"
          yes | "$SDKMANAGER" --licenses >/dev/null 2>&1 || true
          "$SDKMANAGER" "platforms;android-37" "platform-tools"

      - name: Cache Chaquopy
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches/chaquopy
            ~/.cache/pip
          key: chaquopy-${{ runner.os }}-py310-${{ hashFiles('chaquo/requirements.txt', 'gradle/libs.versions.toml') }}
          restore-keys: |
            chaquopy-${{ runner.os }}-py310-

      - name: Configure release signing
        env:
          B64KEY: ${{ secrets.SIGNING_KEY }}
          KEY_ALIAS: ${{ secrets.SIGNING_KEY_ALIAS }}
          STORE_PASSWORD: ${{ secrets.SIGNING_STORE_PASSWORD }}
          KEY_PASSWORD: ${{ secrets.SIGNING_KEY_PASSWORD }}
        run: |
          set -euo pipefail

          for v in B64KEY KEY_ALIAS STORE_PASSWORD KEY_PASSWORD; do
            if [ -z "${!v:-}" ]; then
              echo "::error::缺少 GitHub Secret: $v"
              exit 1
            fi
          done

          STORE_FILE="$GITHUB_WORKSPACE/.key/release.jks"
          mkdir -p "$(dirname "$STORE_FILE")"

          printf '%s' "$B64KEY" | tr -d '\r\n ' | base64 --decode > "$STORE_FILE"
          test -s "$STORE_FILE"

          keytool -list \
            -keystore "$STORE_FILE" \
            -storepass "$STORE_PASSWORD" \
            -alias "$KEY_ALIAS" >/dev/null

          cat > local.properties <<EOF
          storeFile=$STORE_FILE
          keyAlias=$KEY_ALIAS
          storePassword=$STORE_PASSWORD
          keyPassword=$KEY_PASSWORD
          EOF

          echo "Release signing configured."

      - name: Grant execute permission
        run: chmod +x gradlew

      - name: Verify expected build tasks
        run: |
          set -euo pipefail
          ./gradlew :app:tasks --all --no-daemon > app-tasks.txt
          grep -q "assembleMobileRelease" app-tasks.txt
          grep -q "assembleLeanbackRelease" app-tasks.txt

      - name: Build mobile and TV release APKs
        env:
          GRADLE_OPTS: "-Dorg.gradle.internal.http.connectionTimeout=120000 -Dorg.gradle.internal.http.socketTimeout=120000"
        run: |
          set -euo pipefail
          rm -rf Release/apk dist
          mkdir -p Release/apk dist

          ./gradlew \
            :app:assembleMobileRelease \
            :app:assembleLeanbackRelease \
            --stacktrace \
            --no-daemon

          echo "== Raw Release/apk =="
          find Release/apk -maxdepth 1 -type f -name '*.apk' -print -exec ls -lh {} \;

      - name: Collect four APKs
        run: |
          set -euo pipefail

          MOBILE_64="Release/apk/mobile-arm64_v8a.apk"
          MOBILE_32="Release/apk/mobile-armeabi_v7a.apk"
          TV_64="Release/apk/leanback-arm64_v8a.apk"
          TV_32="Release/apk/leanback-armeabi_v7a.apk"

          for f in "$MOBILE_64" "$MOBILE_32" "$TV_64" "$TV_32"; do
            test -f "$f" || {
              echo "::error::缺少 APK: $f"
              echo "实际输出："
              find Release/apk -maxdepth 1 -type f -name '*.apk' -print || true
              exit 1
            }
          done

          cp "$MOBILE_64" "dist/mobile-arm64-v8a.apk"
          cp "$MOBILE_32" "dist/mobile-armeabi-v7a.apk"
          cp "$TV_64" "dist/tv-arm64-v8a.apk"
          cp "$TV_32" "dist/tv-armeabi-v7a.apk"

          COUNT="$(find dist -maxdepth 1 -type f -name '*.apk' | wc -l | tr -d ' ')"
          [ "$COUNT" = "4" ] || {
            echo "::error::实际 APK 数量是 $COUNT，不是 4"
            exit 1
          }

          echo "== Final APKs =="
          ls -lh dist/

      - name: Upload four APKs to Actions
        uses: actions/upload-artifact@v4
        with:
          name: TV-5.6.3-four-apks
          path: dist/*.apk
          if-no-files-found: error
          retention-days: 30

      - name: Publish four APKs to GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail

          TAG="auto-${{ github.run_number }}"
          TITLE="TV 5.6.3 自动构建"

          cat > release-notes.md <<EOF
          TV 5.6.3 自动构建

          Commit: ${{ github.sha }}

          - mobile arm64-v8a
          - mobile armeabi-v7a
          - TV arm64-v8a
          - TV armeabi-v7a
          EOF

          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release delete "$TAG" --cleanup-tag -y
          else
            git push origin ":refs/tags/$TAG" >/dev/null 2>&1 || true
          fi

          gh release create "$TAG" \
            --target "$GITHUB_SHA" \
            --title "$TITLE" \
            --notes-file release-notes.md \
            --latest

          for APK in \
            "dist/mobile-arm64-v8a.apk" \
            "dist/mobile-armeabi-v7a.apk" \
            "dist/tv-arm64-v8a.apk" \
            "dist/tv-armeabi-v7a.apk"
          do
            gh release upload "$TAG" "$APK" --clobber
          done

          echo "== Release assets =="
          gh release view "$TAG" --json assets --jq '.assets[].name'
